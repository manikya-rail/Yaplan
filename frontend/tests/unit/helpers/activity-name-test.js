import { activityName } from '../../../helpers/activity-name';
import { module, test } from 'qunit';

module('Unit | Helper | activity name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activityName(42);
  assert.ok(result);
});
