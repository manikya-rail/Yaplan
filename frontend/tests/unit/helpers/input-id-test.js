import { inputId } from '../../../helpers/input-id';
import { module, test } from 'qunit';

module('Unit | Helper | input id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inputId(42);
  assert.ok(result);
});
