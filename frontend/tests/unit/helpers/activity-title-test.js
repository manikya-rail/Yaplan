import { activityTitle } from '../../../helpers/activity-title';
import { module, test } from 'qunit';

module('Unit | Helper | activity title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activityTitle(42);
  assert.ok(result);
});
