import { toggle } from '../../../helpers/toggle';
import { module, test } from 'qunit';

module('Unit | Helper | toggle');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toggle(42);
  assert.ok(result);
});
