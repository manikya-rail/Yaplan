import { isWorkflowTemplateOwner } from '../../../helpers/is-workflow-template-owner';
import { module, test } from 'qunit';

module('Unit | Helper | is workflow template owner');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isWorkflowTemplateOwner(42);
  assert.ok(result);
});
