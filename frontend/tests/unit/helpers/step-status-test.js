import { stepStatus } from '../../../helpers/step-status';
import { module, test } from 'qunit';

module('Unit | Helper | step status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stepStatus(42);
  assert.ok(result);
});
