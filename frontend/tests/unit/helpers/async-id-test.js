import { asyncId } from '../../../helpers/async-id';
import { module, test } from 'qunit';

module('Unit | Helper | async id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = asyncId(42);
  assert.ok(result);
});
