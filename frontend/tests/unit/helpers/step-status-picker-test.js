import { stepStatusPicker } from '../../../helpers/step-status-picker';
import { module, test } from 'qunit';

module('Unit | Helper | step status picker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stepStatusPicker(42);
  assert.ok(result);
});
