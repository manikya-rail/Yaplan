import { docDate } from '../../../helpers/doc-date';
import { module, test } from 'qunit';

module('Unit | Helper | doc date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = docDate(42);
  assert.ok(result);
});
