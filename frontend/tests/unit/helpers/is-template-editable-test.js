import { isTemplateEditable } from '../../../helpers/is-template-editable';
import { module, test } from 'qunit';

module('Unit | Helper | is template editable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isTemplateEditable(42);
  assert.ok(result);
});
