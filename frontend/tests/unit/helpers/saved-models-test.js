import { savedModel } from '../../../helpers/saved-model';
import { module, test } from 'qunit';

module('Unit | Helper | saved model');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = savedModel(42);
  assert.ok(result);
});
