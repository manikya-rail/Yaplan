import { graphAction } from '../../../helpers/graph-action';
import { module, test } from 'qunit';

module('Unit | Helper | graph action');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = graphAction(42);
  assert.ok(result);
});
