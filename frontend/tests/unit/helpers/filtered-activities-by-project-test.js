import { filteredActivitiesByProject } from '../../../helpers/filtered-activities-by-project';
import { module, test } from 'qunit';

module('Unit | Helper | filtered activities by project');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filteredActivitiesByProject(42);
  assert.ok(result);
});
