import { hasClass } from '../../../helpers/has-class';
import { module, test } from 'qunit';

module('Unit | Helper | has class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasClass(42);
  assert.ok(result);
});
