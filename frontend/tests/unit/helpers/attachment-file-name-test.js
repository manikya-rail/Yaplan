import { attachmentFileName } from '../../../helpers/attachment-file-name';
import { module, test } from 'qunit';

module('Unit | Helper | attachment file name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = attachmentFileName(42);
  assert.ok(result);
});
