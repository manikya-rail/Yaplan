import { idEquality } from '../../../helpers/id-equality';
import { module, test } from 'qunit';

module('Unit | Helper | id equality');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = idEquality(42);
  assert.ok(result);
});
