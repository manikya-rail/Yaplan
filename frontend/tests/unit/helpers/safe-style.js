import { safeStyle } from '../../../helpers/safe-style';
import { module, test } from 'qunit';

module('Unit | Helper | project image style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = safeStyle(42);
  assert.ok(result);
});
