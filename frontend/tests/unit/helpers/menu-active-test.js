import { menuActive } from '../../../helpers/menu-active';
import { module, test } from 'qunit';

module('Unit | Helper | menu active');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = menuActive(42);
  assert.ok(result);
});
