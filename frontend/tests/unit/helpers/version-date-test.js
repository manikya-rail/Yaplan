import { versionDate } from '../../../helpers/version-date';
import { module, test } from 'qunit';

module('Unit | Helper | version date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = versionDate(42);
  assert.ok(result);
});
