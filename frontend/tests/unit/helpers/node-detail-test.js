import { nodeDetail } from '../../../helpers/node-detail';
import { module, test } from 'qunit';

module('Unit | Helper | node detail');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = nodeDetail(42);
  assert.ok(result);
});
