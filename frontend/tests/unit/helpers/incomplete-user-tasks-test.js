import { incompleteUserTasks } from '../../../helpers/incomplete-user-tasks';
import { module, test } from 'qunit';

module('Unit | Helper | incomplete user tasks');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = incompleteUserTasks(42);
  assert.ok(result);
});
