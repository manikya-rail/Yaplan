import { filteredByParam } from '../../../helpers/filtered-by-param';
import { module, test } from 'qunit';

module('Unit | Helper | filtered by param');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filteredByParam(42);
  assert.ok(result);
});
