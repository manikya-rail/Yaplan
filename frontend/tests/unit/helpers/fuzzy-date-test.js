import { fuzzyDate } from '../../../helpers/fuzzy-date';
import { module, test } from 'qunit';

module('Unit | Helper | fuzzy date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fuzzyDate(42);
  assert.ok(result);
});
