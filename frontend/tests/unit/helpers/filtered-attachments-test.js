import { filteredAttachments } from '../../../helpers/filtered-attachments';
import { module, test } from 'qunit';

module('Unit | Helper | filtered attachments');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filteredAttachments(42);
  assert.ok(result);
});
