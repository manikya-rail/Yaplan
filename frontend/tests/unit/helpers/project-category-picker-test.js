import { projectCategoryPicker } from '../../../helpers/project-category-picker';
import { module, test } from 'qunit';

module('Unit | Helper | project category picker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = projectCategoryPicker(42);
  assert.ok(result);
});
