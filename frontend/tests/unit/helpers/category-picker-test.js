import { categoryPicker } from '../../../helpers/category-picker';
import { module, test } from 'qunit';

module('Unit | Helper | category picker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryPicker(42);
  assert.ok(result);
});
